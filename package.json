{
  "name": "playwright-toolshop-demo",
  "version": "1.0.0",
  "description": "Playwright test suite for login, password recovery, registration, and the products page",
  "scripts": {
    "test": "playwright test",
    "test:debug": "playwright test --debug",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.1",
    "@types/node": "^22.5.5"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
